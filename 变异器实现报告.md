# 4.1 变异器实现报告

## 概述

本节仅说明在原有框架基础上**修改**或**新增**的变异策略，共 2 处修改、3 个新增策略，合计 5 项。

---

## 修改的策略

### 1. `interesting_random_bytes` — Interesting Values 替换（修改）

**修改原因：**  
原实现使用的 interesting value 均为普通可打印 ASCII 字符（如 `'A'`、`'OK'`、`'TEST'`），不具备触发整数溢出或边界异常的能力，与 AFL 策略的设计意图不符。

**修改内容：**  
将 interesting value 表替换为 AFL 风格的整数边界值，覆盖常见的溢出临界点；同时引入随机字节序（大端/小端）选取：

```python
interesting_values = {
    1: [0, 1, 16, 32, 64, 100, 127, 128, 200, 254, 255],
    2: [0, 1, 128, 255, 256, 512, 1000, 1024, 4096, 32767, 32768, 65534, 65535],
    4: [0, 1, 128, 255, 256, 512, 65535, 65536, 100000,
        0x7fffffff, 0x80000000, 0xfffffffe, 0xffffffff],
}
endian = random.choice(['big', 'little'])
value_bytes = (value % (256 ** N)).to_bytes(N, byteorder=endian)
```

**适用场景：**  
- 触发整数溢出、符号位翻转（`0x7f` vs `0x80`）、除零（`\x00`）等边界行为。  
- 对 `sample1` 中的 `float(s)` 与除法运算效果显著。

**越界与编码安全：**  
替换位置限定在 `[0, len(data) - N]`；value 通过 `% (256 ** N)` 截断后再 `to_bytes()` 防止长度溢出；以 `errors='ignore'` 解码。

---

### 2. `havoc_random_replace` — Havoc 随机内容替换（Bug 修复）

**修改原因：**  
原实现中当 `length == replace_len` 时（即替换整段内容），`randint(0, 0)` 合法，但代码通过 `if length - replace_len == 0` 分支将替换内容强制设为空字节 `bytearray()`，导致 75% 概率分支实际上将输入清空为空字符串，行为错误。

**修改内容：**  
删除错误的空分支，让 `randint(0, length - replace_len)` 直接处理该边界情况（当 `length == replace_len` 时必然取 `start=0`，即复制整段原文）：

```python
# 修改前（有 bug）
if length - replace_len == 0:
    replace_bytes = bytearray()          # 错误：应复制原文而非置空
else:
    start = random.randint(0, length - replace_len)
    replace_bytes = data[start:start + replace_len]

# 修改后
start = random.randint(0, length - replace_len)
replace_bytes = data[start:start + replace_len]
```

**影响：**  
修复后 75% 概率分支在任何输入长度下均能正确复制原文片段，不再产生非预期的空输出，保证 seed 质量。

---

## 新增的策略

### 3. `delete_random_bytes` — 随机字节删除（新增）

**核心逻辑：**  
从字符串中随机删除连续的 1~8 个字节，始终保留至少 1 个字节，避免产生空字符串。

```python
delete_len = random.randint(1, min(8, length - 1))
pos = random.randint(0, length - delete_len)
new_data = data[:pos] + data[pos + delete_len:]
```

**适用场景：**  
- 探索输入变短后的路径，触发下标越界（如 `sample3` 中 `s[1]`、`s[2]` 在字符串过短时直接抛 `IndexError`）。  
- 原有框架只有插入和替换，缺少"缩短输入"的能力；本策略与插入类操作互补，形成长度双向扰动。

**越界与编码安全：**  
要求 `len(data) >= 2` 才执行；删除长度上限为 `min(8, length - 1)` 确保至少保留 1 字节；以 `errors='ignore'` 解码。

---

### 4. `repeat_random_byte` — 重复字节覆写（新增）

**核心逻辑：**  
随机选取一个 byte 值（50% 取自原字符串中已有字节，50% 取随机可打印 byte），将从随机位置起连续的 N 个字节（N = 1~8）全部覆写为该值。

```python
fill_byte = data[random.randint(0, length - 1)] if random.random() < 0.5 \
            else random.randint(0x20, 0x7E)
for i in range(N):
    data[pos + i] = fill_byte
```

**适用场景：**  
- 制造 `AAAA`、`\x00\x00\x00` 等重复模式，触发 `sample3` 中 `s.index("L")` 找不到目标时抛出的 `ValueError`，以及 off-by-one 等重复字符相关路径。  
- 与 `interesting_random_bytes` 互补：后者覆盖整数边界值，本策略覆盖重复填充模式。

**越界与编码安全：**  
N 上限为 `min(8, length)`，`pos` 限定在 `[0, length - N]`；fill byte 来自原数组时必然合法，随机 byte 限定在可打印 ASCII 范围；以 `errors='ignore'` 解码。

---

### 5. `insert_special_token` — 特殊边界 Token 插入（新增）

**核心逻辑：**  
维护一张针对实验样例定制的特殊 token 字典 `_SPECIAL_TOKENS`，在字符串的随机位置插入随机选取的 token：

```python
token = random.choice(_SPECIAL_TOKENS)
pos   = random.randint(0, len(s))
return s[:pos] + token + s[pos:]
```

字典覆盖以下类别：

| 类别 | 示例 token | 对应样例 |
|---|---|---|
| 控制字符 | `\x00`、`\n`、`\t` | 通用 |
| 格式化字符串 | `%s`、`%d`、`{Key}`、`{}` | `sample2`（`temp % len(s)` / `.format()`）|
| 浮点边界 | `inf`、`nan`、`1e308`、`-1e308` | `sample1`（`float(s)` / 除零）|
| HTML 特殊字符 | `<`、`>`、`&`、`<!--`、`/>` | `sample4`（`HTMLParser`）|
| 分隔符 | `.`、`..`、`....` | `sample2`（`s.split(".")`）|
| 长重复串 | `'A' * 32`、`'\x00' * 4` | 索引越界 |

**适用场景：**  
- 纯随机字节变异很难偶然生成 `inf`、`{Key}`、`<!--` 等结构化 token；本策略以字典驱动方式直接投放这些高价值输入，大幅提升对格式约束程序的覆盖效率。  
- 与 `havoc_random_insert` 互补：后者注重长度与结构扰动，本策略注重语义触发。

**越界与编码安全：**  
token 均为合法 Python `str`，字符串拼接不涉及字节越界；插入位置限定在 `[0, len(s)]`，始终合法。

---

## 输入多样性保障

新增的 3 个策略从三个维度补充了原有框架的覆盖盲区：

| 维度 | 原有框架 | 补充后 |
|---|---|---|
| 长度变化 | 只有插入/替换（长度不减） | 加入 `delete_random_bytes`，实现双向扰动 |
| 重复模式 | 无 | 加入 `repeat_random_byte`，覆盖填充/重复路径 |
| 语义触发 | 纯随机字节，难以命中格式约束 | 加入 `insert_special_token`，字典驱动精准投放 |

所有字节级操作均遵循统一编码安全模式：`bytearray.encode('utf-8', errors='ignore')` → 变异 → `decode('utf-8', errors='ignore')`，保证变异结果始终是合法 `str`，不会因编码异常导致框架崩溃。
