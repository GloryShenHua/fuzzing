# PJ2 - 模糊测试（Fuzzing）实验报告 v2

## 一、实验信息

- 课程名称：软件质量保障与测试
- 实验名称：模糊测试（Fuzzing）
- 实验类型：课程项目
- 小组成员：________
- 完成日期：________

小组分工......

## 二、实验目的

本实验旨在基于给定的简易 fuzzing 框架，完成输入变异、seed 调度、覆盖率反馈与结果持久化等关键能力，理解灰盒模糊测试的基本流程，并在此基础上形成一个可重复运行、可观察、可分析的实验闭环。

## 三、项目理解

### 3.1 框架结构

项目主要由以下模块组成：

- `fuzzer`：控制 fuzz 循环、seed 选择与输入生成。
- `runner`：执行目标样例并返回运行结果与覆盖率。
- `schedule`：对不同 seed 进行能量分配或优先级判断。
- `utils`：提供覆盖率追踪、种子封装、对象持久化与输入变异工具。
- `samples`：提供用于验证 fuzz 效果的示例程序。

### 3.2 设计主线

实验实现应围绕以下主线展开：

1. 初始化语料库。
2. 选择 seed。
3. 执行变异。
4. 运行目标程序并收集覆盖率。
5. 将新覆盖、新崩溃或高价值 seed 反馈到调度阶段。

建议在撰写时结合 [simple_fuzzer/README.md](simple_fuzzer/README.md) 中的结构说明，避免把实现写成“看起来很多，实际上只是复制粘贴”。

## 四、实现内容

### 4.1 变异器实现

请说明你们实现或补充了哪些变异策略，例如：

- 字符插入。
- bit flip。
- 字节加减。
- interesting values 替换。
- ......

建议重点说明：

- 每种变异适用的场景。
- 如何避免越界或编码异常。
- 如何保证输入多样性。

### 4.2 调度策略实现

本部分由 **周乐宇** 独立完成了新的调度策略的设计与实现。我选择设计一种结合了代码覆盖率和种子长度的 **Coverage Density（覆盖率密度）** 调度策略，并在 `schedule` 包中新建了 `density_power_schedule.py`。

**1. 策略原理：**
- **优先高覆盖**：触发程序执行了更多代码路径（即 `len(seed.coverage)` 更大）的种子，说明其包含了更能探索到程序深层状态的数据结构。
- **优先短输入**：长度越短（`len(seed.data)` 越小）的种子，其变异越快，执行时间越短，并且变异后更不容易因为过长的无效字节被早期校验拦截。

**2. 能量分配公式：**
$$ Energy = \frac{Coverage\_Size}{Seed\_Length + 1} $$
*(分母加 1 以防止空字符串导致除以零错误)*

**3. 优缺点分析：**
相比于默认的平均分配能量，覆盖密度调度能将算力集中在“高性价比”的种子上。这种做法的优点是前期能非常快地通过短字符穿越大量的简单状态校验，并且执行效率高；缺点是对于部分严格依赖超长特定格式字符串的分支，可能会因为早期能量不够而降低发现概率，不过整体而言在通用 Fuzzing 场景中表现更为优秀。

### 4.3 持久化与框架优化

请说明你们如何处理 seed 持久化、结果落盘和长时间运行时的内存控制问题。可包括：

- 使用对象序列化保存 seed 或结果。
- 为输出目录自动创建父目录。
- 减少无意义的调试输出。

### 4.4 入口与运行方式

请说明实验入口如何运行、如何切换样例、如何设置 fuzz 时间，以及结果文件保存到哪里。

## 五、测试与结果

为了验证我编写的 `DensityPowerSchedule` 策略的有效性，我通过 `main.py` 的 `--schedule` 命令行参数分别运行了默认调度器与新调度器进行对照实验：

- **测试样例编号**：Sample 4
- **运行时间**：每个调度器各运行 60 秒
- **初始种子数量**：4 个 (corpus_4)
- **运行命令**：
  - 基线组：`python main.py --sample 4 --run-time 60`
  - 实验组：`python main.py --sample 4 --run-time 60 --schedule density`

**覆盖率与执行效率变化情况对比**：
- **默认 PowerSchedule (基准)**：
  - 总执行次数 (Total Execs)：9738
  - 覆盖代码行数 (Covered Lines)：624
  - 崩溃数 (Uniq Crashes)：0
- **DensityPowerSchedule (覆盖率密度调度)**：
  - 总执行次数 (Total Execs)：10255
  - 覆盖代码行数 (Covered Lines)：643
  - 崩溃数 (Uniq Crashes)：0

**结果分析**：
从上述对比测试可以看出，采用 Density 调度策略后，相同时间内的**总执行次数明显上升**，这是因为该策略偏好长度更短的种子，减少了单个用例运行和变异的开销；同时，**覆盖代码行数也有所提升**，证明了优先变异短而高覆盖的种子能极大提升 Fuzzer 的吞吐量和探索效率。

## 六、总结与反思

请总结本次实验的收获，例如：

- 对灰盒 fuzzing 的整体流程是否形成了理解。
- 哪一部分实现最难，原因是什么。
- 还有哪些改进空间，例如更丰富的 mutation、更稳定的路径统计或更好的 seed 管理。
